<h2 class="activity-title"><a href="<%= to @tabi.path %>"><%= @tabi.title %></a> &gt; <span class="current-activity"><%= @activity.title %></span></h2>
<div class="contents">
  <div class="edit-btn">
    <a href="<%= @activity.edit_path %>"><i class="fa fa-pencil"></i>編集</a>
  </div>
  <%- if @activity.memo && @activity.memo.length > 0 -%>
  <p class="memo"><%== rich_text(@activity.memo) %></p>
  <%- end -%>
<%- if @activity.has_location? -%>
  <div>
    <a href="http://maps.google.com/maps?q=<%= @activity.location %>" target="_blank" class="secondary-btn"><i class="fa fa-map-marker"></i>地図アプリで見る</a>
  </div>
<div class="js-map" data-location="<%= @activity.location %>" style="width: 100%; height: 230px"></div>
<%- end -%>

  <%- unless @activity.has_any_detail? -%>
    <p>編集して，この行き先に説明や場所を追加すると便利です．</p>
  <%- end -%>
</div>
